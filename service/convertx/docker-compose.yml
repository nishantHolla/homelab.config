services:
  convertx:
    image: ghcr.io/c4illin/convertx:latest

    container_name: convertx

    ports:
      - ${CONVERTX_PORT}:3000/tcp

    volumes:
      - ${HOMELAB_DATA_DIR}/convertx:/app/data

    env_file:
      - .env

    environment:
      TZ: ${TZ}

    restart: unless-stopped
