# Uptime Kuma

## Installation

- Create `.env` file with the following values
```bash
# Permission mask (can be 0022)
UMASK=
```

- Run `docker compose up`

- visit `http://<homelab-ip>:<UPTIME_KUMA_PORT>` to create the admin account
